.row
	.col-md-4.col-md-offset-4
		.row
			.col-md-12.text-center
				h2.text-primary
					|Log in
		.row
			.row
				form(ng-submit='changeView()')
					input.form-control(autofocus type='text' placeholder='Username' ng-model='username')
			a(href='#!/signUp')
				| Click here if you don't have an account